<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.items.dao.ItemDao">

    <select id="selectItemById" resultType="com.example.items.domain.Item">
        select *
        from items
        where itemid=#{itemid};
    </select>

    <select id="getAllItems" resultType="com.example.items.domain.Item">
        select *
        from items
    </select>
</mapper>

